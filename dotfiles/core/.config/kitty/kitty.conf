# Kitty config.

font_family JetBrainsMono Nerd Font Mono
font_size 14.0

# Show ligatures (e.g. --- as a long dash) as their separate characters when
# the cursor is on them.
disable_ligatures cursor

# Draw Material Design icons from the non-Mono Nerd Font, which has larger icons
# than the single-cell Mono variant.
symbol_map U+F0001-U+F1AF0 JetBrainsMono Nerd Font

cursor_shape beam
cursor_blink_interval 0

active_tab_font_style bold

tab_bar_edge top
tab_bar_min_tabs 1
tab_bar_margin_width 4
tab_bar_margin_height 0 0
tab_bar_style custom

window_padding_width 0
window_margin_width 1 6 1

allow_remote_control no
shell_integration disabled
enable_audio_bell no
window_alert_on_bell no
visual_bell_duration 0.1
bell_on_tab "󰂚 "

# Don't draw progress bars reported by programs with OSC 9;4 (e.g. nvim for
# LSP progress, which flashes on every save).
progress_bar hidden

mouse_map super+left click grabbed,ungrabbed mouse_click_url

detect_urls yes
url_style single
url_excluded_characters <>

include gruvbox_dark.conf
include kitty.${KITTY_OS}.conf

